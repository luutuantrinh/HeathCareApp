<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tdc.edu.vn.heathcareapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HeathCareApp">
        <activity android:name=".LoginSupportActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UserProfileActivity" />
        <activity android:name=".ConversationDetailActivity" />
        <activity android:name=".LikesActivity" />
        <activity android:name=".NutritionListActivity" />
        <activity android:name=".DetailNutritionTowerActivity" />
        <activity android:name=".DetailNewsActivity" />
        <activity android:name=".DetailPostActivity" />
        <activity android:name=".ConversationListActivity" />
        <activity android:name=".NewsActivity" />
        <activity android:name=".SettingsNotificationActivity" />
        <activity android:name=".NotificationsActivity" />
        <activity android:name=".CreateContentActivity" />
        <activity android:name=".AddFriendActivity">

        </activity>
        <activity android:name=".NutritionActivity" /> <!-- <activity android:name=".NewFeedActivity" /> -->
        <activity android:name=".MainActivity">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
        <activity android:name=".NewFeedActivity" />
    </application>

</manifest>